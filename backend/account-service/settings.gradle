rootProject.name = 'account-service'

def contractsPath = System.getenv("CONTRACTS_PATH") ?: "../contracts"

includeBuild(contractsPath) {
    dependencySubstitution {
        substitute module("com.banking:contracts") using project(":")
    }
}